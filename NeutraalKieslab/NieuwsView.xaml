<UserControl x:Class="NeutraalKieslab.NieuwsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">

    <UserControl.Resources>
        <!-- Colors to match website -->
        <SolidColorBrush x:Key="PrimaryBlue" Color="#2C3E50"/>
        <SolidColorBrush x:Key="LightBlue" Color="#5A6C7D"/>
        <SolidColorBrush x:Key="AccentGreen" Color="#27AE60"/>
        <SolidColorBrush x:Key="LightGray" Color="#ECF0F1"/>
        <SolidColorBrush x:Key="DarkGray" Color="#7F8C8D"/>
        <SolidColorBrush x:Key="BreakingRed" Color="#E53E3E"/>

        <!-- News Card Style like website -->
        <Style x:Key="WebsiteNewsCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Margin" Value="20,15"/>
            <Setter Property="Padding" Value="30"/>
            <Setter Property="MinHeight" Value="120"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="LightGray" Direction="270" ShadowDepth="2" Opacity="0.4"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="Gray" Direction="270" ShadowDepth="4" Opacity="0.6"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Filter Button Style -->
        <Style x:Key="FilterButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryBlue}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBlue}"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryBlue}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Background="{StaticResource LightGray}">

            <!-- Header like website -->
            <Border Background="White" Padding="40,30" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Laatste Nieuws over Nederlandse Politiek" 
                              FontSize="32" FontWeight="Bold" 
                              Foreground="{StaticResource PrimaryBlue}" 
                              HorizontalAlignment="Center" Margin="0,0,0,20"/>

                    <!-- Filter buttons -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="Alle Nieuws" Style="{StaticResource FilterButtonStyle}" 
                               Click="FilterAll" Tag="All"/>
                        <Button Content="🔴 Breaking" Style="{StaticResource FilterButtonStyle}" 
                               Click="FilterBreaking" Tag="Breaking"/>
                        <Button Content="🏛️ Regering" Style="{StaticResource FilterButtonStyle}" 
                               Click="FilterByCategory" Tag="Regering"/>
                        <Button Content="📊 Peilingen" Style="{StaticResource FilterButtonStyle}" 
                               Click="FilterByCategory" Tag="Peilingen"/>
                        <Button Content="🌍 Buitenland" Style="{StaticResource FilterButtonStyle}" 
                               Click="FilterByCategory" Tag="Buitenlandbeleid"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Dynamic News Container -->
            <StackPanel x:Name="NewsContainer" Margin="40,20,40,40">
                <!-- News articles will be populated here dynamically -->
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>